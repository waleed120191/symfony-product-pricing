<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('js/jquery-3.3.1.js') }}"></script>
        {% endblock %}
    </head>
    <body>

    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
        <h5 class="my-0 mr-md-auto font-weight-normal">Gelato test</h5>
        <nav class="my-2 my-md-0 mr-md-3">
            <a class="p-2 text-dark" href="{{ url('pricing_rule_index') }}">Pricing rule</a>
            <a class="p-2 text-dark" href="{{ url('product_index') }}">Product</a>
        </nav>
        <a class="btn btn-outline-primary" href="{{ url('product_front__index') }}">Front</a>
    </div>

    <div class="container">
        {% block body %}{% endblock %}
    </div>

    </body>
</html>
